{
  "name": "fueldock-estonia",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "save-server": "node server.js",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && cd dist && git add -A && git commit -m 'deploy' && git push -f https://github.com/uvemae/fueldock-estonia-1.git master:gh-pages && cd .."
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.78.0",
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "leaflet": "^1.9.4",
    "vue": "^3.4.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "autoprefixer": "^10.4.0",
    "postcss": "^8.4.0",
    "tailwindcss": "^3.4.0",
    "vite": "^5.0.0"
  }
}
